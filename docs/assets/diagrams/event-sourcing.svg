<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1100 720" role="img" aria-label="Event Sourcing — state = fold(log) — Append-only EventLog + content-addressed ContentStore → fold → four state slices (data-flow diagram)">
<style>

    /* ==========================================================
       THEME VARIABLES — switch by toggling [data-theme] on the html root
       ========================================================== */
    svg, :root, [data-theme="dark"] {
      --bg: #020617;
      --grid: #1e293b;
      --text: #ffffff;
      --text-muted: #94a3b8;
      --text-dim: #475569;
      /* UI chrome (menu hints, footer) needs more contrast than SVG .t-dim accents */
      --text-faint: #7d8da1;

      --panel: rgba(15, 23, 42, 0.5);
      --panel-border: #1e293b;
      --lane-fill: rgba(15, 23, 42, 0.22);
      --lane-stroke: #334155;

      --arrow: #64748b;
      --arrow-emphasis: #34d399;

      /* Opaque mask color used behind semi-transparent component fills
         so the arrows drawn underneath are properly hidden. */
      --mask: #0f172a;

      --frontend-fill:    rgba(8, 51, 68, 0.4);
      --frontend-stroke:  #22d3ee;
      --backend-fill:     rgba(6, 78, 59, 0.4);
      --backend-stroke:   #34d399;
      --database-fill:    rgba(76, 29, 149, 0.4);
      --database-stroke:  #a78bfa;
      --cloud-fill:       rgba(120, 53, 15, 0.3);
      --cloud-stroke:     #fbbf24;
      --security-fill:    rgba(136, 19, 55, 0.4);
      --security-stroke:  #fb7185;
      --messagebus-fill:  rgba(251, 146, 60, 0.3);
      --messagebus-stroke:#fb923c;
      --external-fill:    rgba(30, 41, 59, 0.5);
      --external-stroke:  #94a3b8;

      --toolbar-bg:       rgba(15, 23, 42, 0.8);
      --toolbar-border:   #334155;
      --toolbar-text:     #e2e8f0;
      --toolbar-hover:    rgba(15, 23, 42, 0.95);
      --toolbar-menu-bg:  #0f172a;
    }

    @media (prefers-color-scheme: light) {
      svg {
      --bg: #f8fafc;
      --grid: #e2e8f0;
      --text: #0f172a;
      --text-muted: #64748b;
      --text-dim: #94a3b8;
      --text-faint: #64748b;

      --panel: #ffffff;
      --panel-border: #e2e8f0;
      --lane-fill: rgba(248, 250, 252, 0.65);
      --lane-stroke: #cbd5e1;

      --arrow: #94a3b8;
      --arrow-emphasis: #059669;

      --mask: #ffffff;

      --frontend-fill:    rgba(34, 211, 238, 0.15);
      --frontend-stroke:  #0891b2;
      --backend-fill:     rgba(52, 211, 153, 0.18);
      --backend-stroke:   #059669;
      --database-fill:    rgba(167, 139, 250, 0.2);
      --database-stroke:  #7c3aed;
      --cloud-fill:       rgba(251, 191, 36, 0.18);
      --cloud-stroke:     #d97706;
      --security-fill:    rgba(251, 113, 133, 0.15);
      --security-stroke:  #e11d48;
      --messagebus-fill:  rgba(251, 146, 60, 0.15);
      --messagebus-stroke:#ea580c;
      --external-fill:    rgba(148, 163, 184, 0.18);
      --external-stroke:  #64748b;

      --toolbar-bg:       rgba(255, 255, 255, 0.92);
      --toolbar-border:   #cbd5e1;
      --toolbar-text:     #334155;
      --toolbar-hover:    #ffffff;
      --toolbar-menu-bg:  #ffffff;
      }
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, 'DejaVu Sans Mono', 'Liberation Mono', 'Noto Sans Mono CJK SC', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', monospace;
      background: var(--bg);
      min-height: 100vh;
      padding: 2rem;
      color: var(--text);
      transition: background 0.2s ease, color 0.2s ease;
    }

    .container { max-width: 1200px; margin: 0 auto; }

    .header { margin-bottom: 2rem; }

    .header-row {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 0.5rem;
    }

    .pulse-dot {
      width: 12px;
      height: 12px;
      background: var(--frontend-stroke);
      border-radius: 50%;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.5; }
    }

    h1 {
      font-size: 1.5rem;
      font-weight: 700;
      letter-spacing: -0.025em;
    }

    .subtitle {
      color: var(--text-muted);
      font-size: 0.875rem;
      margin-left: 1.75rem;
    }

    .diagram-container {
      background: var(--panel);
      border-radius: 1rem;
      border: 1px solid var(--panel-border);
      padding: 1.5rem;
      overflow-x: auto;
    }

    svg {
      width: 100%;
      min-width: min(900px, 100%);
      display: block;
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1rem;
      margin-top: 2rem;
    }

    .card {
      background: var(--panel);
      border-radius: 0.75rem;
      border: 1px solid var(--panel-border);
      padding: 1.25rem;
    }

    .card-header {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-bottom: 0.75rem;
    }

    .card-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
    }

    .card-dot.cyan     { background: var(--frontend-stroke); }
    .card-dot.emerald  { background: var(--backend-stroke); }
    .card-dot.violet   { background: var(--database-stroke); }
    .card-dot.amber    { background: var(--cloud-stroke); }
    .card-dot.rose     { background: var(--security-stroke); }
    .card-dot.orange   { background: var(--messagebus-stroke); }
    .card-dot.slate    { background: var(--external-stroke); }

    .card h3 { font-size: 0.875rem; font-weight: 600; color: var(--text); }

    .card ul {
      list-style: none;
      color: var(--text-muted);
      font-size: 0.75rem;
    }

    .card li { margin-bottom: 0.375rem; }

    .footer {
      text-align: center;
      margin-top: 1.5rem;
      color: var(--text-faint);
      font-size: 0.75rem;
    }

    /* ==========================================================
       Print stylesheet — hide interactive controls, force light,
       drop the grid so the diagram doesn't waste ink, use landscape
       so wide diagrams fit, pack summary cards into 2 columns to
       avoid an orphan card on a trailing page.
       ========================================================== */
    @page { size: landscape; margin: 1.5cm; }

    @media print {
      /* Force the FULL light palette — including component fills/strokes and
         lane/arrow colors — so printing from dark theme doesn't put neon
         strokes and translucent dark fills on white paper. */
      svg, :root, [data-theme="dark"], [data-theme="light"] {
        --bg: #ffffff;
        --grid: transparent;
        --panel: #ffffff;
        --panel-border: #e2e8f0;
        --text: #0f172a;
        --text-muted: #475569;
        --text-dim: #94a3b8;
        --text-faint: #64748b;
        --mask: #ffffff;
        --lane-fill: rgba(248, 250, 252, 0.65);
        --lane-stroke: #cbd5e1;
        --arrow: #94a3b8;
        --arrow-emphasis: #059669;
        --frontend-fill:    rgba(34, 211, 238, 0.15);
        --frontend-stroke:  #0891b2;
        --backend-fill:     rgba(52, 211, 153, 0.18);
        --backend-stroke:   #059669;
        --database-fill:    rgba(167, 139, 250, 0.2);
        --database-stroke:  #7c3aed;
        --cloud-fill:       rgba(251, 191, 36, 0.18);
        --cloud-stroke:     #d97706;
        --security-fill:    rgba(251, 113, 133, 0.15);
        --security-stroke:  #e11d48;
        --messagebus-fill:  rgba(251, 146, 60, 0.15);
        --messagebus-stroke:#ea580c;
        --external-fill:    rgba(148, 163, 184, 0.18);
        --external-stroke:  #64748b;
      }
      body { background: #ffffff !important; padding: 0; }
      .container { max-width: none; }
      .toolbar, .archify-toast, .no-print { display: none !important; }
      .diagram-container, .card { box-shadow: none; border-color: #e2e8f0; }
      .cards { grid-template-columns: 1fr 1fr; }
      /* Ensure elements break sensibly across pages */
      .card { break-inside: avoid; page-break-inside: avoid; }
      .diagram-container { break-inside: avoid; page-break-inside: avoid; }
      h1, .subtitle { color: #0f172a; }
    }

    /* ==========================================================
       TOOLBAR (theme toggle + export menu)
       ========================================================== */
    .toolbar {
      position: fixed;
      top: 1rem;
      right: 1rem;
      display: flex;
      gap: 0.5rem;
      z-index: 100;
    }
    .toolbar button {
      background: var(--toolbar-bg);
      color: var(--toolbar-text);
      border: 1px solid var(--toolbar-border);
      padding: 0.5rem 0.875rem;
      border-radius: 0.5rem;
      font-family: inherit;
      font-size: 0.75rem;
      font-weight: 500;
      cursor: pointer;
      backdrop-filter: blur(8px);
      transition: background 0.15s, border-color 0.15s;
      display: inline-flex;
      align-items: center;
      gap: 0.375rem;
    }
    .toolbar button:hover {
      background: var(--toolbar-hover);
      border-color: var(--arrow);
    }
    .toolbar button:focus-visible {
      outline: 2px solid var(--frontend-stroke);
      outline-offset: 2px;
    }
    .toolbar .export-wrap { position: relative; }
    .toolbar .export-menu {
      position: absolute;
      top: calc(100% + 0.375rem);
      right: 0;
      background: var(--toolbar-menu-bg);
      border: 1px solid var(--toolbar-border);
      border-radius: 0.5rem;
      padding: 0.375rem;
      min-width: 160px;
      display: none;
      flex-direction: column;
      box-shadow: 0 4px 20px rgba(0,0,0,0.25);
    }
    .toolbar .export-menu.open { display: flex; }
    .toolbar .export-menu button {
      background: transparent;
      border: 0;
      justify-content: space-between;
      width: 100%;
      text-align: left;
      padding: 0.5rem 0.75rem;
    }
    .toolbar .export-menu button:hover {
      background: var(--toolbar-hover);
    }
    .toolbar .export-menu .hint {
      color: var(--text-faint);
      font-size: 0.625rem;
    }
    .toolbar .export-menu hr {
      border: 0;
      border-top: 1px solid var(--toolbar-border);
      margin: 0.25rem 0.375rem;
      opacity: 0.5;
    }

    /* Toast — brief feedback for actions like "copied" */
    .archify-toast {
      position: fixed;
      top: 1rem;
      left: 50%;
      transform: translateX(-50%) translateY(-8px);
      background: var(--toolbar-menu-bg);
      color: var(--text);
      border: 1px solid var(--toolbar-border);
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      font-family: inherit;
      font-size: 0.75rem;
      opacity: 0;
      transition: opacity 0.2s, transform 0.2s;
      z-index: 200;
      pointer-events: none;
      box-shadow: 0 4px 20px rgba(0,0,0,0.25);
    }
    .archify-toast.show {
      opacity: 1;
      transform: translateX(-50%) translateY(0);
    }

    @media (max-width: 720px) {
      body { padding: 1rem; }
      .toolbar {
        position: static;
        justify-content: flex-end;
        margin-bottom: 1rem;
      }
      .header { margin-bottom: 1.25rem; }
      .header-row {
        gap: 0.75rem;
        align-items: flex-start;
      }
      h1 {
        font-size: 1.25rem;
        line-height: 1.25;
      }
      .subtitle {
        margin-left: 0;
        font-size: 0.8125rem;
        line-height: 1.55;
      }
      .diagram-container {
        padding: 0.75rem;
        border-radius: 0.75rem;
      }
      .cards {
        grid-template-columns: 1fr;
        margin-top: 1rem;
      }
    }

    /* ==========================================================
       SVG SEMANTIC CLASSES — use these instead of inline fill/stroke
       ========================================================== */
    .c-grid       { stroke: var(--grid); fill: none; }
    .c-mask       { fill: var(--mask); stroke: none; }

    .c-frontend   { fill: var(--frontend-fill);   stroke: var(--frontend-stroke); }
    .c-backend    { fill: var(--backend-fill);    stroke: var(--backend-stroke); }
    .c-database   { fill: var(--database-fill);   stroke: var(--database-stroke); }
    .c-cloud      { fill: var(--cloud-fill);      stroke: var(--cloud-stroke); }
    .c-security   { fill: var(--security-fill);   stroke: var(--security-stroke); }
    .c-messagebus { fill: var(--messagebus-fill); stroke: var(--messagebus-stroke); }
    .c-external   { fill: var(--external-fill);   stroke: var(--external-stroke); }

    /* Text helpers */
    .t-primary { fill: var(--text); }
    .t-muted   { fill: var(--text-muted); }
    .t-dim     { fill: var(--text-dim); }
    .t-frontend   { fill: var(--frontend-stroke); }
    .t-backend    { fill: var(--backend-stroke); }
    .t-database   { fill: var(--database-stroke); }
    .t-cloud      { fill: var(--cloud-stroke); }
    .t-security   { fill: var(--security-stroke); }
    .t-messagebus { fill: var(--messagebus-stroke); }
    .t-external   { fill: var(--external-stroke); }

    /* Arrows */
    .a-default   { stroke: var(--arrow); fill: none; }
    .a-emphasis  { stroke: var(--arrow-emphasis); fill: none; }
    .a-security  { stroke: var(--security-stroke); fill: none; stroke-dasharray: 5,5; }
    .a-dashed    { stroke: var(--database-stroke); fill: none; stroke-dasharray: 4,4; }

    /* Arrowhead markers reference these */
    .m-default   { fill: var(--arrow); }
    .m-emphasis  { fill: var(--arrow-emphasis); }
    .m-security  { fill: var(--security-stroke); }
    .m-dashed    { fill: var(--database-stroke); }

    /* Dashed boundary variants */
    .c-security-group { fill: transparent; stroke: var(--security-stroke); stroke-dasharray: 4,4; }
    .c-region         { fill: rgba(251, 191, 36, 0.05); stroke: var(--cloud-stroke); stroke-dasharray: 8,4; }
    .c-lane           { fill: var(--lane-fill); stroke: var(--lane-stroke); stroke-dasharray: 6,6; }

    /* Optional trace animation, enabled only by meta.animation = "trace".
       Static diagrams do not carry data-animation and render exactly as before. */
    svg[data-animation="trace"] [data-animate="edge"] {
      stroke-dasharray: 10 8;
      stroke-dashoffset: 54;
      animation: archify-edge-flow 2.4s linear infinite;
      animation-delay: calc(var(--step, 0) * 160ms);
    }
    svg[data-animation="trace"] [data-animate="node"] {
      transform-box: fill-box;
      transform-origin: center;
      animation: archify-node-pulse 3.6s ease-in-out infinite;
      animation-delay: calc(var(--step, 0) * 160ms);
    }
    @keyframes archify-edge-flow {
      to { stroke-dashoffset: 0; }
    }
    @keyframes archify-node-pulse {
      0%, 72%, 100% { filter: none; stroke-width: 1.5; }
      18%, 36% {
        filter: drop-shadow(0 0 8px var(--arrow-emphasis));
        stroke-width: 2.4;
      }
    }
    @media (prefers-reduced-motion: reduce) {
      svg[data-animation="trace"] [data-animate] {
        animation: none !important;
        stroke-dashoffset: 0;
      }
    }
  
</style>
        <!-- Definitions -->
        <defs>
          <marker id="arrowhead" markerWidth="10" markerHeight="7" refX="9" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" class="m-default" />
          </marker>
          <marker id="arrowhead-emphasis" markerWidth="10" markerHeight="7" refX="9" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" class="m-emphasis" />
          </marker>
          <marker id="arrowhead-security" markerWidth="10" markerHeight="7" refX="9" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" class="m-security" />
          </marker>
          <marker id="arrowhead-dashed" markerWidth="10" markerHeight="7" refX="9" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" class="m-dashed" />
          </marker>
          <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 40 0 L 0 0 0 40" class="c-grid" stroke-width="0.5"/>
          </pattern>
        </defs>

        <!-- Background Grid -->
        <rect width="100%" height="100%" fill="url(#grid)" />

        <!-- Data Stages -->
        <rect x="16" y="46" width="168" height="600" rx="10" class="c-lane" stroke-width="1"/>
        <text x="100" y="68" class="t-dim" font-size="9" font-weight="600" text-anchor="middle">01 / Writers</text>

        <rect x="231" y="46" width="168" height="600" rx="10" class="c-lane" stroke-width="1"/>
        <text x="315" y="68" class="t-dim" font-size="9" font-weight="600" text-anchor="middle">02 / Append</text>

        <rect x="446" y="46" width="168" height="600" rx="10" class="c-lane" stroke-width="1"/>
        <text x="530" y="68" class="t-dim" font-size="9" font-weight="600" text-anchor="middle">03 / Store</text>

        <rect x="661" y="46" width="168" height="600" rx="10" class="c-lane" stroke-width="1"/>
        <text x="745" y="68" class="t-dim" font-size="9" font-weight="600" text-anchor="middle">04 / Fold</text>

        <rect x="876" y="46" width="168" height="600" rx="10" class="c-lane" stroke-width="1"/>
        <text x="960" y="68" class="t-dim" font-size="9" font-weight="600" text-anchor="middle">05 / State</text>

        <!-- Flow paths -->
        <path d="M 156 157 L 207.5 157 L 207.5 271 L 259 271" class="a-emphasis" stroke-width="1.8" marker-end="url(#arrowhead-emphasis)"/>
        <path d="M 156 271 L 259 271" class="a-emphasis" stroke-width="1.8" marker-end="url(#arrowhead-emphasis)"/>
        <path d="M 156 385 L 207.5 385 L 207.5 271 L 259 271" class="a-emphasis" stroke-width="1.8" marker-end="url(#arrowhead-emphasis)"/>
        <path d="M 156 499 L 207.5 499 L 207.5 271 L 259 271" class="a-emphasis" stroke-width="1.8" marker-end="url(#arrowhead-emphasis)"/>
        <path d="M 371 271 L 474 271" class="a-dashed" stroke-width="1.4" marker-end="url(#arrowhead-dashed)"/>
        <path d="M 371 271 L 689 271" class="a-emphasis" stroke-width="1.8" marker-end="url(#arrowhead-emphasis)"/>
        <path d="M 586 271 L 689 271" class="a-dashed" stroke-width="1.4" marker-end="url(#arrowhead-dashed)"/>
        <path d="M 801 271 L 852.5 271 L 852.5 157 L 904 157" class="a-emphasis" stroke-width="1.8" marker-end="url(#arrowhead-emphasis)"/>
        <path d="M 801 271 L 904 271" class="a-emphasis" stroke-width="1.8" marker-end="url(#arrowhead-emphasis)"/>
        <path d="M 801 271 L 852.5 271 L 852.5 385 L 904 385" class="a-emphasis" stroke-width="1.8" marker-end="url(#arrowhead-emphasis)"/>
        <path d="M 801 271 L 852.5 271 L 852.5 499 L 904 499" class="a-emphasis" stroke-width="1.8" marker-end="url(#arrowhead-emphasis)"/>

        <!-- Nodes -->
        <rect x="44" y="128" width="112" height="58" rx="6" class="c-mask"/>
        <rect x="44" y="128" width="112" height="58" rx="6" class="c-backend" stroke-width="1.5"/>
        <text x="100" y="149" class="t-primary" font-size="10" font-weight="600" text-anchor="middle">Engine</text>
        <text x="100" y="165" class="t-muted" font-size="7" text-anchor="middle">writes RuntimeState</text>

        <rect x="44" y="242" width="112" height="58" rx="6" class="c-mask"/>
        <rect x="44" y="242" width="112" height="58" rx="6" class="c-backend" stroke-width="1.5"/>
        <text x="100" y="263" class="t-primary" font-size="10" font-weight="600" text-anchor="middle">Policy</text>
        <text x="100" y="279" class="t-muted" font-size="7" text-anchor="middle">TaskStatePatch</text>

        <rect x="44" y="356" width="112" height="58" rx="6" class="c-mask"/>
        <rect x="44" y="356" width="112" height="58" rx="6" class="c-backend" stroke-width="1.5"/>
        <text x="100" y="377" class="t-primary" font-size="10" font-weight="600" text-anchor="middle">Composer</text>
        <text x="100" y="393" class="t-muted" font-size="7" text-anchor="middle">writes ContextState</text>

        <rect x="44" y="470" width="112" height="58" rx="6" class="c-mask"/>
        <rect x="44" y="470" width="112" height="58" rx="6" class="c-backend" stroke-width="1.5"/>
        <text x="100" y="491" class="t-primary" font-size="10" font-weight="600" text-anchor="middle">fold()</text>
        <text x="100" y="507" class="t-muted" font-size="7" text-anchor="middle">writes GovernanceState</text>

        <rect x="259" y="242" width="112" height="58" rx="6" class="c-mask"/>
        <rect x="259" y="242" width="112" height="58" rx="6" class="c-messagebus" stroke-width="1.5"/>
        <text x="315" y="263" class="t-primary" font-size="10" font-weight="600" text-anchor="middle">EventLog</text>
        <text x="315" y="279" class="t-muted" font-size="7" text-anchor="middle">append-only stream</text>
        <text x="315" y="289" class="t-messagebus" font-size="7" text-anchor="middle">≤ 4 KB payload</text>

        <rect x="474" y="242" width="112" height="58" rx="6" class="c-mask"/>
        <rect x="474" y="242" width="112" height="58" rx="6" class="c-database" stroke-width="1.5"/>
        <text x="530" y="263" class="t-primary" font-size="10" font-weight="600" text-anchor="middle">ContentStore</text>
        <text x="530" y="279" class="t-muted" font-size="7" text-anchor="middle">content-addressed</text>
        <text x="530" y="289" class="t-database" font-size="7" text-anchor="middle">SHA-256 dedup</text>

        <rect x="689" y="242" width="112" height="58" rx="6" class="c-mask"/>
        <rect x="689" y="242" width="112" height="58" rx="6" class="c-backend" stroke-width="1.5"/>
        <text x="745" y="263" class="t-primary" font-size="10" font-weight="600" text-anchor="middle">fold(stream)</text>
        <text x="745" y="279" class="t-muted" font-size="7" text-anchor="middle">deterministic replay</text>

        <rect x="904" y="128" width="112" height="58" rx="6" class="c-mask"/>
        <rect x="904" y="128" width="112" height="58" rx="6" class="c-frontend" stroke-width="1.5"/>
        <text x="960" y="149" class="t-primary" font-size="10" font-weight="600" text-anchor="middle">RuntimeState</text>
        <text x="960" y="165" class="t-muted" font-size="7" text-anchor="middle">messages · usage</text>

        <rect x="904" y="242" width="112" height="58" rx="6" class="c-mask"/>
        <rect x="904" y="242" width="112" height="58" rx="6" class="c-frontend" stroke-width="1.5"/>
        <text x="960" y="263" class="t-primary" font-size="10" font-weight="600" text-anchor="middle">TaskState</text>
        <text x="960" y="279" class="t-muted" font-size="7" text-anchor="middle">todos · active content</text>

        <rect x="904" y="356" width="112" height="58" rx="6" class="c-mask"/>
        <rect x="904" y="356" width="112" height="58" rx="6" class="c-frontend" stroke-width="1.5"/>
        <text x="960" y="377" class="t-primary" font-size="10" font-weight="600" text-anchor="middle">ContextState</text>
        <text x="960" y="393" class="t-muted" font-size="7" text-anchor="middle">plan ref · compaction</text>

        <rect x="904" y="470" width="112" height="58" rx="6" class="c-mask"/>
        <rect x="904" y="470" width="112" height="58" rx="6" class="c-frontend" stroke-width="1.5"/>
        <text x="960" y="491" class="t-primary" font-size="10" font-weight="600" text-anchor="middle">GovernanceState</text>
        <text x="960" y="507" class="t-muted" font-size="7" text-anchor="middle">cost · tokens · denied</text>

        <!-- Flow labels -->
        <rect x="179.45" y="193" width="56.1" height="16" rx="4" class="c-mask"/>
        <text x="207.5" y="204" class="t-backend" font-size="8" text-anchor="middle">envelopes</text>
        <rect x="169.65" y="250" width="75.7" height="16" rx="4" class="c-mask"/>
        <text x="207.5" y="261" class="t-backend" font-size="8" text-anchor="middle">state patches</text>
        <rect x="169.65" y="307" width="75.7" height="16" rx="4" class="c-mask"/>
        <text x="207.5" y="318" class="t-backend" font-size="8" text-anchor="middle">context plans</text>
        <rect x="177" y="364" width="61" height="16" rx="4" class="c-mask"/>
        <text x="207.5" y="375" class="t-backend" font-size="8" text-anchor="middle">governance</text>
        <rect x="379.75" y="250" width="85.5" height="27" rx="4" class="c-mask"/>
        <text x="422.5" y="261" class="t-messagebus" font-size="8" text-anchor="middle">large bodies</text>
        <text x="422.5" y="272" class="t-dim" font-size="7" text-anchor="middle">ContentRef only</text>
        <rect x="509.3" y="227" width="41.400000000000006" height="16" rx="4" class="c-mask"/>
        <text x="530" y="238" class="t-backend" font-size="8" text-anchor="middle">stream</text>
        <rect x="616.8" y="250" width="41.400000000000006" height="16" rx="4" class="c-mask"/>
        <text x="637.5" y="261" class="t-messagebus" font-size="8" text-anchor="middle">derefs</text>
        <rect x="834.25" y="193" width="36.5" height="16" rx="4" class="c-mask"/>
        <text x="852.5" y="204" class="t-backend" font-size="8" text-anchor="middle">slice</text>
        <rect x="834.25" y="250" width="36.5" height="16" rx="4" class="c-mask"/>
        <text x="852.5" y="261" class="t-backend" font-size="8" text-anchor="middle">slice</text>
        <rect x="834.25" y="307" width="36.5" height="16" rx="4" class="c-mask"/>
        <text x="852.5" y="318" class="t-backend" font-size="8" text-anchor="middle">slice</text>
        <rect x="834.25" y="364" width="36.5" height="16" rx="4" class="c-mask"/>
        <text x="852.5" y="375" class="t-backend" font-size="8" text-anchor="middle">slice</text>

        <!-- Legend -->
        <text x="214" y="664" class="t-primary" font-size="10" font-weight="600">Legend</text>
        <path d="M 214 684 L 248 684" class="a-emphasis" stroke-width="1.8" marker-end="url(#arrowhead-emphasis)"/>
        <text x="257" y="687" class="t-muted" font-size="8">primary data</text>
        <path d="M 340 684 L 374 684" class="a-security" stroke-width="1.4" marker-end="url(#arrowhead-security)"/>
        <text x="383" y="687" class="t-muted" font-size="8">policy / PII</text>
        <path d="M 480 684 L 514 684" class="a-dashed" stroke-width="1.4" marker-end="url(#arrowhead-dashed)"/>
        <text x="523" y="687" class="t-muted" font-size="8">async batch</text>
        <rect x="625" y="676" width="14" height="9" rx="2" class="c-database" stroke-width="1"/>
        <text x="646" y="684" class="t-muted" font-size="8">data store</text>
      </svg>