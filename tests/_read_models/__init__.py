"""Pure read-model projections over a session's EventLog, shared by the tests.

Kept out of the runtime packages on purpose: these are observation shapes the
tests assert against, not something the engine depends on.
"""
