# Patterns

- Extract function: name the intent, move the body, leave a call site.
- Introduce parameter object when an argument list grows past three.
- Replace conditional with polymorphism only when a third case appears.
