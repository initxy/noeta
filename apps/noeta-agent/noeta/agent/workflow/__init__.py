"""Workflow: template-driven multi-task sessions.

- templates.py: validation and placeholder utilities for templates / workflow templates
- transcript.py: extracts a task conversation transcript from the noeta EventLog (handoff material)
- handoff.py: standalone handoff generation on advance (direct gateway call; prefills next-node params + summary)
- service.py: workflow-session advance orchestration (preview / confirm) and node goal assembly
"""
