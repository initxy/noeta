# Claude Code local private config (skills travel with the project; not ignored)
.claude/settings.local.json
.claude/*.local.*
.claude/worktrees/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.import_linter_cache/
.tox/
htmlcov/
.coverage
.coverage.*
dist/
build/
site/
*.egg
node_modules/

# VitePress
docs/.vitepress/cache/
docs/.vitepress/dist/

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local demo / scratch artifacts
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.tmp.xml
*.tmp

# Leftover pre-platform local config (may contain an api_key). Nothing reads it
# any more — the platform is env-only (apps/noeta-agent/.env, see
# docs/reference/configuration.md) — but keep it ignored so a stale local copy
# can never be committed.
/noeta.config.json

# Local artifacts from Playwright MCP test runs (console logs / page snapshots)
/.playwright-mcp/
