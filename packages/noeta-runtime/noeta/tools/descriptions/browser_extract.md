Extracts a snapshot of the current page in the sandbox browser: its readable text (as Markdown) followed by a numbered list of the interactive elements (links, buttons, inputs) with the `index` you use to click or type.

Use it to read a page and to obtain the element indices before `browser_click` or `browser_type`.
