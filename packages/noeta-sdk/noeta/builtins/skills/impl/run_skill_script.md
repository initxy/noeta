Run an active skill's bundled script via an allowlisted interpreter (bash, python3, or node). The script must already be discovered under an active skill in the workspace; argv is built by Noeta (interpreter + real path + validated args), never from a free-form command string, and args may not contain shell metacharacters. cwd is the workspace root; bounded timeout and output cap apply; not sandboxed (trusted workspace only).
