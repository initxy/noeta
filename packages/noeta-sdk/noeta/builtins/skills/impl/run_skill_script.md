Run a script bundled with an ACTIVE skill; its file extension picks the interpreter (bash, python3, or node). The arguments are passed as argv with no shell in between, so they may not contain shell metacharacters. Runs with cwd at the workspace root under a timeout, and returns the tail of stdout and stderr — a non-zero exit or a timeout comes back as an error carrying the same output.
