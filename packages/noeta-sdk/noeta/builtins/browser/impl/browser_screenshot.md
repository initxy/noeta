Captures a PNG screenshot of the current page in the sandbox browser and saves it as a workspace artifact for the user to view; the image is not fed back to you.

Use it when you need to record or show the visual state of the page; for reading page content or finding elements to act on, use `browser_extract` instead.
