Captures a PNG screenshot of the current page in the sandbox browser and attaches it to the result for the user to view — no file is written to the workspace, and the image is not fed back to you.

Use it when you need to record or show the visual state of the page; for reading page content or finding elements to act on, use `browser_extract` instead.
